version: '3.8'

services:
  trend-radar:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./storage/charts:/app/storage/charts
      - ./storage/reports:/app/storage/reports
    restart: always